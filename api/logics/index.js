const ping      = global.abs('api/logics/ping');
const home      = global.abs('api/logics/home');
const auth      = global.abs('api/logics/auth');
const session   = global.abs('api/logics/session');
const volunteer = global.abs('api/logics/volunteer');
const dashboard = global.abs('api/logics/dashboard');

/* ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ */
module.exports = {
  ping,
  home,
  auth,
  session,
  volunteer,
  dashboard,
};
