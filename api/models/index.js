const Volunteer  = global.abs('api/models/volunteer');
const Session    = global.abs('api/models/session');
const Attendance = global.abs('api/models/attendance');

/* ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ */
module.exports = {
  Volunteer,
  Session,
  Attendance,
};
